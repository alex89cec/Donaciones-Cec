# Notas del proyecto — Donaciones Streaming CEC Liceo Militar

## Preferencias
- **Email admin del proyecto: `amedinar89@gmail.com`** — usar siempre este mail
  para el usuario admin (Firebase Authentication) y en las reglas de Firestore
  (`firestore.rules`). No usar placeholders ni otros emails.

## Datos del proyecto
- **Dominio:** `streamcec.com` (GitHub Pages, deploy automático desde `main`).
- **Firebase:** proyecto `streamcec-1950` (Firestore + Authentication). Config
  pública en `firebase-shared.js`.
- **Rama de trabajo:** `claude/rugby-donation-site-srsqth`; se publica llevándola
  a `main` (fast-forward), que dispara el deploy.
- **Colores del club:** azul marino + dorado (del escudo) + blanco 🤍.
